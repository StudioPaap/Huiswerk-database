CREATE TABLE user (
    id serial,
    name varchar(255),
    password varchar(10),

);

CREATE TABLE product (
    id serial,
    name varchar(255),
    brand varchar(255),
    price double,
    current_stock int,
    sold int,
    primary key(id)
);